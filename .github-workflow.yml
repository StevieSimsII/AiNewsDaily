name: Legacy Deploy Method (Deprecated)

# This file is kept for reference but is no longer used
# Please use .github/workflows/deploy-github-pages.yml instead

on:
  push:
    branches:
      - none  # Disabled

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is no longer used
        run: echo "This workflow is deprecated. See .github/workflows/deploy-github-pages.yml"
